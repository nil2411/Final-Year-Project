# KrishiSaathi Backend - Environment Variables

# Server
PORT=5000
NODE_ENV=development
CORS_ORIGINS=*
DEFAULT_LANGUAGE=en

# OpenAI
OPENAI_API_KEY=your_openai_api_key_here
# Optional: override model
# OPENAI_MODEL=gpt-4o-mini

# Google Cloud Project (for Translate API)
# Set GOOGLE_APPLICATION_CREDENTIALS to the local path of your service account JSON key
GOOGLE_PROJECT_ID=your_gcp_project_id_here
# Example (set outside .env):
# setx GOOGLE_APPLICATION_CREDENTIALS "C:\\path\\to\\service-account.json"
